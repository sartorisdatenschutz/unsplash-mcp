# Unsplash API credentials
UNSPLASH_ACCESS_KEY=your_access_key_here

# Docker network shared with Nginx Proxy Manager.
# Change this if your NPM installation uses another external network name.
PROXY_NETWORK=npm

# Optional when running server.py directly
MCP_TRANSPORT=stdio
MCP_HOST=0.0.0.0
MCP_PORT=8000
