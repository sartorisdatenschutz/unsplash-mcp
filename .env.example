# Unsplash API credentials
UNSPLASH_ACCESS_KEY=your_access_key_here

# Bearer token required for remote HTTP access
MCP_API_KEY=generate_a_long_random_secret_here

# Optional when running server.py directly
MCP_TRANSPORT=stdio
MCP_HOST=0.0.0.0
MCP_PORT=8000
